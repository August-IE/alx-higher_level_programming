-- A script that computes the average of all records in the table second_table
-- of the database hbtn_0c_0 in my MySQL server.
-- The result column should be average
SELECT AVG(`score`) AS `average`
FROM `second_table`;
